import React from "react";
import { WithStyles, withStyles } from "@material-ui/core";

const styles = () => ({
  root: {},
});

const {{pascalCase name}}: React.FC<WithStyles<typeof styles>> = (props) => {
  const { classes, ...rest } = props;
  return <div {...rest} />;
};

export default withStyles(styles, { name: "{{pascalCase name}}" })({{pascalCase name}});
